<div class="container my-3">
  <h1><%= @user.name %>さんのプロフィール編集</h1>

  <%= form_with model: @user, local: true do |f| %>
    <%= render "public/users/edit_form", f: f %>
    <div class="form-group">
      <%= f.submit "編集する", class: "btn btn-success" %>
    </div>
  <% end %>
</div>